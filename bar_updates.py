import resource_urls


# ----------------------------------------------------------------------------------------------------------------------
# gets current player health and updates hp bar image on screen according to the health value from 0-100
def health_bar_update(character):
    if character.health == 100:
        return resource_urls.health_100_url
    if character.health == 99:
        return resource_urls.health_99_url
    if character.health == 98:
        return resource_urls.health_98_url
    if character.health == 97:
        return resource_urls.health_97_url
    if character.health == 96:
        return resource_urls.health_96_url
    if character.health == 95:
        return resource_urls.health_95_url
    if character.health == 94:
        return resource_urls.health_94_url
    if character.health == 93:
        return resource_urls.health_93_url
    if character.health == 92:
        return resource_urls.health_92_url
    if character.health == 91:
        return resource_urls.health_91_url
    if character.health == 90:
        return resource_urls.health_90_url
    if character.health == 89:
        return resource_urls.health_89_url
    if character.health == 88:
        return resource_urls.health_88_url
    if character.health == 87:
        return resource_urls.health_87_url
    if character.health == 86:
        return resource_urls.health_86_url
    if character.health == 85:
        return resource_urls.health_85_url
    if character.health == 84:
        return resource_urls.health_84_url
    if character.health == 83:
        return resource_urls.health_83_url
    if character.health == 82:
        return resource_urls.health_82_url
    if character.health == 81:
        return resource_urls.health_81_url
    if character.health == 80:
        return resource_urls.health_80_url
    if character.health == 79:
        return resource_urls.health_79_url
    if character.health == 78:
        return resource_urls.health_78_url
    if character.health == 77:
        return resource_urls.health_77_url
    if character.health == 76:
        return resource_urls.health_76_url
    if character.health == 75:
        return resource_urls.health_75_url
    if character.health == 74:
        return resource_urls.health_74_url
    if character.health == 73:
        return resource_urls.health_73_url
    if character.health == 72:
        return resource_urls.health_72_url
    if character.health == 71:
        return resource_urls.health_71_url
    if character.health == 70:
        return resource_urls.health_70_url
    if character.health == 69:
        return resource_urls.health_69_url
    if character.health == 68:
        return resource_urls.health_68_url
    if character.health == 67:
        return resource_urls.health_67_url
    if character.health == 66:
        return resource_urls.health_66_url
    if character.health == 65:
        return resource_urls.health_65_url
    if character.health == 64:
        return resource_urls.health_64_url
    if character.health == 63:
        return resource_urls.health_63_url
    if character.health == 62:
        return resource_urls.health_62_url
    if character.health == 61:
        return resource_urls.health_61_url
    if character.health == 60:
        return resource_urls.health_60_url
    if character.health == 59:
        return resource_urls.health_59_url
    if character.health == 58:
        return resource_urls.health_58_url
    if character.health == 57:
        return resource_urls.health_57_url
    if character.health == 56:
        return resource_urls.health_56_url
    if character.health == 55:
        return resource_urls.health_55_url
    if character.health == 54:
        return resource_urls.health_54_url
    if character.health == 53:
        return resource_urls.health_53_url
    if character.health == 52:
        return resource_urls.health_52_url
    if character.health == 51:
        return resource_urls.health_51_url
    if character.health == 50:
        return resource_urls.health_50_url
    if character.health == 49:
        return resource_urls.health_49_url
    if character.health == 48:
        return resource_urls.health_48_url
    if character.health == 47:
        return resource_urls.health_47_url
    if character.health == 46:
        return resource_urls.health_46_url
    if character.health == 45:
        return resource_urls.health_45_url
    if character.health == 44:
        return resource_urls.health_44_url
    if character.health == 43:
        return resource_urls.health_43_url
    if character.health == 42:
        return resource_urls.health_42_url
    if character.health == 41:
        return resource_urls.health_41_url
    if character.health == 40:
        return resource_urls.health_40_url
    if character.health == 39:
        return resource_urls.health_39_url
    if character.health == 38:
        return resource_urls.health_38_url
    if character.health == 37:
        return resource_urls.health_37_url
    if character.health == 36:
        return resource_urls.health_36_url
    if character.health == 35:
        return resource_urls.health_35_url
    if character.health == 34:
        return resource_urls.health_34_url
    if character.health == 33:
        return resource_urls.health_33_url
    if character.health == 32:
        return resource_urls.health_32_url
    if character.health == 31:
        return resource_urls.health_31_url
    if character.health == 30:
        return resource_urls.health_30_url
    if character.health == 29:
        return resource_urls.health_29_url
    if character.health == 28:
        return resource_urls.health_28_url
    if character.health == 27:
        return resource_urls.health_27_url
    if character.health == 26:
        return resource_urls.health_26_url
    if character.health == 25:
        return resource_urls.health_25_url
    if character.health == 24:
        return resource_urls.health_24_url
    if character.health == 23:
        return resource_urls.health_23_url
    if character.health == 22:
        return resource_urls.health_22_url
    if character.health == 21:
        return resource_urls.health_21_url
    if character.health == 20:
        return resource_urls.health_20_url
    if character.health == 19:
        return resource_urls.health_19_url
    if character.health == 18:
        return resource_urls.health_18_url
    if character.health == 17:
        return resource_urls.health_17_url
    if character.health == 16:
        return resource_urls.health_16_url
    if character.health == 15:
        return resource_urls.health_15_url
    if character.health == 14:
        return resource_urls.health_14_url
    if character.health == 13:
        return resource_urls.health_13_url
    if character.health == 12:
        return resource_urls.health_12_url
    if character.health == 11:
        return resource_urls.health_11_url
    if character.health == 10:
        return resource_urls.health_10_url
    if character.health == 9:
        return resource_urls.health_9_url
    if character.health == 8:
        return resource_urls.health_8_url
    if character.health == 7:
        return resource_urls.health_7_url
    if character.health == 6:
        return resource_urls.health_6_url
    if character.health == 5:
        return resource_urls.health_5_url
    if character.health == 4:
        return resource_urls.health_4_url
    if character.health == 3:
        return resource_urls.health_3_url
    if character.health == 2:
        return resource_urls.health_2_url
    if character.health == 1:
        return resource_urls.health_1_url
    if character.health == 0:
        return resource_urls.health_0_url

    else:
        return resource_urls.health_0_url


# player energy bar update
def energy_bar_update(character):
    if character.energy == 100:
        return resource_urls.energy_100_url
    if character.energy == 99:
        return resource_urls.energy_99_url
    if character.energy == 98:
        return resource_urls.energy_98_url
    if character.energy == 97:
        return resource_urls.energy_97_url
    if character.energy == 96:
        return resource_urls.energy_96_url
    if character.energy == 95:
        return resource_urls.energy_95_url
    if character.energy == 94:
        return resource_urls.energy_94_url
    if character.energy == 93:
        return resource_urls.energy_93_url
    if character.energy == 92:
        return resource_urls.energy_92_url
    if character.energy == 91:
        return resource_urls.energy_91_url
    if character.energy == 90:
        return resource_urls.energy_90_url
    if character.energy == 89:
        return resource_urls.energy_89_url
    if character.energy == 88:
        return resource_urls.energy_88_url
    if character.energy == 87:
        return resource_urls.energy_87_url
    if character.energy == 86:
        return resource_urls.energy_86_url
    if character.energy == 85:
        return resource_urls.energy_85_url
    if character.energy == 84:
        return resource_urls.energy_84_url
    if character.energy == 83:
        return resource_urls.energy_83_url
    if character.energy == 82:
        return resource_urls.energy_82_url
    if character.energy == 81:
        return resource_urls.energy_81_url
    if character.energy == 80:
        return resource_urls.energy_80_url
    if character.energy == 79:
        return resource_urls.energy_79_url
    if character.energy == 78:
        return resource_urls.energy_78_url
    if character.energy == 77:
        return resource_urls.energy_77_url
    if character.energy == 76:
        return resource_urls.energy_76_url
    if character.energy == 75:
        return resource_urls.energy_75_url
    if character.energy == 74:
        return resource_urls.energy_74_url
    if character.energy == 73:
        return resource_urls.energy_73_url
    if character.energy == 72:
        return resource_urls.energy_72_url
    if character.energy == 71:
        return resource_urls.energy_71_url
    if character.energy == 70:
        return resource_urls.energy_70_url
    if character.energy == 69:
        return resource_urls.energy_69_url
    if character.energy == 68:
        return resource_urls.energy_68_url
    if character.energy == 67:
        return resource_urls.energy_67_url
    if character.energy == 66:
        return resource_urls.energy_66_url
    if character.energy == 65:
        return resource_urls.energy_65_url
    if character.energy == 64:
        return resource_urls.energy_64_url
    if character.energy == 63:
        return resource_urls.energy_63_url
    if character.energy == 62:
        return resource_urls.energy_62_url
    if character.energy == 61:
        return resource_urls.energy_61_url
    if character.energy == 60:
        return resource_urls.energy_60_url
    if character.energy == 59:
        return resource_urls.energy_59_url
    if character.energy == 58:
        return resource_urls.energy_58_url
    if character.energy == 57:
        return resource_urls.energy_57_url
    if character.energy == 56:
        return resource_urls.energy_56_url
    if character.energy == 55:
        return resource_urls.energy_55_url
    if character.energy == 54:
        return resource_urls.energy_54_url
    if character.energy == 53:
        return resource_urls.energy_53_url
    if character.energy == 52:
        return resource_urls.energy_52_url
    if character.energy == 51:
        return resource_urls.energy_51_url
    if character.energy == 50:
        return resource_urls.energy_50_url
    if character.energy == 49:
        return resource_urls.energy_49_url
    if character.energy == 48:
        return resource_urls.energy_48_url
    if character.energy == 47:
        return resource_urls.energy_47_url
    if character.energy == 46:
        return resource_urls.energy_46_url
    if character.energy == 45:
        return resource_urls.energy_45_url
    if character.energy == 44:
        return resource_urls.energy_44_url
    if character.energy == 43:
        return resource_urls.energy_43_url
    if character.energy == 42:
        return resource_urls.energy_42_url
    if character.energy == 41:
        return resource_urls.energy_41_url
    if character.energy == 40:
        return resource_urls.energy_40_url
    if character.energy == 39:
        return resource_urls.energy_39_url
    if character.energy == 38:
        return resource_urls.energy_38_url
    if character.energy == 37:
        return resource_urls.energy_37_url
    if character.energy == 36:
        return resource_urls.energy_36_url
    if character.energy == 35:
        return resource_urls.energy_35_url
    if character.energy == 34:
        return resource_urls.energy_34_url
    if character.energy == 33:
        return resource_urls.energy_33_url
    if character.energy == 32:
        return resource_urls.energy_32_url
    if character.energy == 31:
        return resource_urls.energy_31_url
    if character.energy == 30:
        return resource_urls.energy_30_url
    if character.energy == 29:
        return resource_urls.energy_29_url
    if character.energy == 28:
        return resource_urls.energy_28_url
    if character.energy == 27:
        return resource_urls.energy_27_url
    if character.energy == 26:
        return resource_urls.energy_26_url
    if character.energy == 25:
        return resource_urls.energy_25_url
    if character.energy == 24:
        return resource_urls.energy_24_url
    if character.energy == 23:
        return resource_urls.energy_23_url
    if character.energy == 22:
        return resource_urls.energy_22_url
    if character.energy == 21:
        return resource_urls.energy_21_url
    if character.energy == 20:
        return resource_urls.energy_20_url
    if character.energy == 19:
        return resource_urls.energy_19_url
    if character.energy == 18:
        return resource_urls.energy_18_url
    if character.energy == 17:
        return resource_urls.energy_17_url
    if character.energy == 16:
        return resource_urls.energy_16_url
    if character.energy == 15:
        return resource_urls.energy_15_url
    if character.energy == 14:
        return resource_urls.energy_14_url
    if character.energy == 13:
        return resource_urls.energy_13_url
    if character.energy == 12:
        return resource_urls.energy_12_url
    if character.energy == 11:
        return resource_urls.energy_11_url
    if character.energy == 10:
        return resource_urls.energy_10_url
    if character.energy == 9:
        return resource_urls.energy_9_url
    if character.energy == 8:
        return resource_urls.energy_8_url
    if character.energy == 7:
        return resource_urls.energy_7_url
    if character.energy == 6:
        return resource_urls.energy_6_url
    if character.energy == 5:
        return resource_urls.energy_5_url
    if character.energy == 4:
        return resource_urls.energy_4_url
    if character.energy == 3:
        return resource_urls.energy_3_url
    if character.energy == 2:
        return resource_urls.energy_2_url
    if character.energy == 1:
        return resource_urls.energy_1_url
    if character.energy == 0:
        return resource_urls.energy_0_url

    else:
        return resource_urls.energy_0_url


# player xp bar update
def xp_bar_update(character):
    if character.experience == 100:
        return resource_urls.xp_100_url
    if character.experience == 99:
        return resource_urls.xp_99_url
    if character.experience == 98:
        return resource_urls.xp_98_url
    if character.experience == 97:
        return resource_urls.xp_97_url
    if character.experience == 96:
        return resource_urls.xp_96_url
    if character.experience == 95:
        return resource_urls.xp_95_url
    if character.experience == 94:
        return resource_urls.xp_94_url
    if character.experience == 93:
        return resource_urls.xp_93_url
    if character.experience == 92:
        return resource_urls.xp_92_url
    if character.experience == 91:
        return resource_urls.xp_91_url
    if character.experience == 90:
        return resource_urls.xp_90_url
    if character.experience == 89:
        return resource_urls.xp_89_url
    if character.experience == 88:
        return resource_urls.xp_88_url
    if character.experience == 87:
        return resource_urls.xp_87_url
    if character.experience == 86:
        return resource_urls.xp_86_url
    if character.experience == 85:
        return resource_urls.xp_85_url
    if character.experience == 84:
        return resource_urls.xp_84_url
    if character.experience == 83:
        return resource_urls.xp_83_url
    if character.experience == 82:
        return resource_urls.xp_82_url
    if character.experience == 81:
        return resource_urls.xp_81_url
    if character.experience == 80:
        return resource_urls.xp_80_url
    if character.experience == 79:
        return resource_urls.xp_79_url
    if character.experience == 78:
        return resource_urls.xp_78_url
    if character.experience == 77:
        return resource_urls.xp_77_url
    if character.experience == 76:
        return resource_urls.xp_76_url
    if character.experience == 75:
        return resource_urls.xp_75_url
    if character.experience == 74:
        return resource_urls.xp_74_url
    if character.experience == 73:
        return resource_urls.xp_73_url
    if character.experience == 72:
        return resource_urls.xp_72_url
    if character.experience == 71:
        return resource_urls.xp_71_url
    if character.experience == 70:
        return resource_urls.xp_70_url
    if character.experience == 69:
        return resource_urls.xp_69_url
    if character.experience == 68:
        return resource_urls.xp_68_url
    if character.experience == 67:
        return resource_urls.xp_67_url
    if character.experience == 66:
        return resource_urls.xp_66_url
    if character.experience == 65:
        return resource_urls.xp_65_url
    if character.experience == 64:
        return resource_urls.xp_64_url
    if character.experience == 63:
        return resource_urls.xp_63_url
    if character.experience == 62:
        return resource_urls.xp_62_url
    if character.experience == 61:
        return resource_urls.xp_61_url
    if character.experience == 60:
        return resource_urls.xp_60_url
    if character.experience == 59:
        return resource_urls.xp_59_url
    if character.experience == 58:
        return resource_urls.xp_58_url
    if character.experience == 57:
        return resource_urls.xp_57_url
    if character.experience == 56:
        return resource_urls.xp_56_url
    if character.experience == 55:
        return resource_urls.xp_55_url
    if character.experience == 54:
        return resource_urls.xp_54_url
    if character.experience == 53:
        return resource_urls.xp_53_url
    if character.experience == 52:
        return resource_urls.xp_52_url
    if character.experience == 51:
        return resource_urls.xp_51_url
    if character.experience == 50:
        return resource_urls.xp_50_url
    if character.experience == 49:
        return resource_urls.xp_49_url
    if character.experience == 48:
        return resource_urls.xp_48_url
    if character.experience == 47:
        return resource_urls.xp_47_url
    if character.experience == 46:
        return resource_urls.xp_46_url
    if character.experience == 45:
        return resource_urls.xp_45_url
    if character.experience == 44:
        return resource_urls.xp_44_url
    if character.experience == 43:
        return resource_urls.xp_43_url
    if character.experience == 42:
        return resource_urls.xp_42_url
    if character.experience == 41:
        return resource_urls.xp_41_url
    if character.experience == 40:
        return resource_urls.xp_40_url
    if character.experience == 39:
        return resource_urls.xp_39_url
    if character.experience == 38:
        return resource_urls.xp_38_url
    if character.experience == 37:
        return resource_urls.xp_37_url
    if character.experience == 36:
        return resource_urls.xp_36_url
    if character.experience == 35:
        return resource_urls.xp_35_url
    if character.experience == 34:
        return resource_urls.xp_34_url
    if character.experience == 33:
        return resource_urls.xp_33_url
    if character.experience == 32:
        return resource_urls.xp_32_url
    if character.experience == 31:
        return resource_urls.xp_31_url
    if character.experience == 30:
        return resource_urls.xp_30_url
    if character.experience == 29:
        return resource_urls.xp_29_url
    if character.experience == 28:
        return resource_urls.xp_28_url
    if character.experience == 27:
        return resource_urls.xp_27_url
    if character.experience == 26:
        return resource_urls.xp_26_url
    if character.experience == 25:
        return resource_urls.xp_25_url
    if character.experience == 24:
        return resource_urls.xp_24_url
    if character.experience == 23:
        return resource_urls.xp_23_url
    if character.experience == 22:
        return resource_urls.xp_22_url
    if character.experience == 21:
        return resource_urls.xp_21_url
    if character.experience == 20:
        return resource_urls.xp_20_url
    if character.experience == 19:
        return resource_urls.xp_19_url
    if character.experience == 18:
        return resource_urls.xp_18_url
    if character.experience == 17:
        return resource_urls.xp_17_url
    if character.experience == 16:
        return resource_urls.xp_16_url
    if character.experience == 15:
        return resource_urls.xp_15_url
    if character.experience == 14:
        return resource_urls.xp_14_url
    if character.experience == 13:
        return resource_urls.xp_13_url
    if character.experience == 12:
        return resource_urls.xp_12_url
    if character.experience == 11:
        return resource_urls.xp_11_url
    if character.experience == 10:
        return resource_urls.xp_10_url
    if character.experience == 9:
        return resource_urls.xp_9_url
    if character.experience == 8:
        return resource_urls.xp_8_url
    if character.experience == 7:
        return resource_urls.xp_7_url
    if character.experience == 6:
        return resource_urls.xp_6_url
    if character.experience == 5:
        return resource_urls.xp_5_url
    if character.experience == 4:
        return resource_urls.xp_4_url
    if character.experience == 3:
        return resource_urls.xp_3_url
    if character.experience == 2:
        return resource_urls.xp_2_url
    if character.experience == 1:
        return resource_urls.xp_1_url
    if character.experience == 0:
        return resource_urls.xp_0_url

    else:
        return resource_urls.xp_0_url
